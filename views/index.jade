extends layout

block content
    div
        h1= title
        p Welcome to #{title}

        h1 Set Up A Currency Alert

        form
        fieldset
            legend Select your desired currency
                div.form-group
                    label(for="from") From Currency
                    select.form-control
                        each country in availableCountries
                            option(value=country.value)= country.symbol + ' ' + country.name

                div.form-group
                    label(for="from") To currency
                    select.form-control
                        each country in availableCountries
                            option(value=country.value)= country.symbol + ' ' + country.name

                div.form-group
                    label Required Conversion rate
                    input.form-control


        button.px4.py2.btn.btn-default(type="submit") Submit
